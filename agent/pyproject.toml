[project]
name = "agent"
version = "0.1.0"
description = "CLI agent that generates a React frontend from a natural-language spec"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain-anthropic>=0.3.0",
    "langgraph>=0.2.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    "rich>=13.7.0",
]
